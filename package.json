{
  "name": "apextech-root",
  "version": "1.0.0",
  "description": "Root orchestrator for ApexTech Metais ERP Lab Railway deployment",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "postinstall": "npm run install:backend && npm run install:frontend && npm run build:frontend && npm run build:backend",
    "start": "npm run start --prefix backend"
  }
}
